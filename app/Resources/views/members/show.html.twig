{% extends 'base.html.twig' %}

{% block body %}
    <h1>Member</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ member.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ member.name }}</td>
            </tr>
            <tr>
                <th>Registered as</th>
                <td>{{ user.username }}</td>
            </tr>
        <tr>
            <th>User's tasks</th>
            {% for task in tasks %}
            <td>{{ task.taskname }}</td>
            {% endfor %}
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('homepage') }}">Back to the homepage</a>
        </li>

    </ul>
{% endblock %}
