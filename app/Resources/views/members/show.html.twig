{% extends 'base.html.twig' %}

{% block body %}
    <h1>Member</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ member.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ member.name }}</td>
            </tr>
            <tr>
                <th>Registered as</th>
                <td>{{ user.username }}</td>
            </tr>
        <tr>
            <th>User's team(s)</th>
            {#{% for team in user %}#}
            {#<td>{{  }}</td>#}
            {#{% endfor %}#}
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('members_index') }}">Back to the list</a>
        </li>

    </ul>
{% endblock %}
